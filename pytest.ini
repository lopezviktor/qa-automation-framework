[pytest]
testpaths = tests
addopts = -q
markers =
    smoke: fast checks fr basic API availability
    regression: broader coverage tests
    contract: schema/contract validation tests
    security: security-oriented checks (no leaks, headers, etc)

    